---
coverage:
  status:
    project:
      default:
        target: 60%
        threshold: auto
    patch:
      default:
        target: 70%

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false
