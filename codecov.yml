---
coverage:
  status:
    project:
      default:
        target: 75%
        threshold: auto
    patch:
      default:
        target: 80%

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false
